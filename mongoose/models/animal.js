// import mongoose from "mongoose";
let array = ["srijon","ram","shyam"]
let rando = array[Math.floor(Math.random()*array.length)]
console.log(rando)
// const animalSchema = new mongoose.Schema({
//     name: Math.random[array],
//     desc: String,
//     ishidden: Boolean,
//     days: Number
// });

// // Model: "animals" is the collection name in MongoDB
// const animals = mongoose.model("animals", animalSchema);

// export default animals;
